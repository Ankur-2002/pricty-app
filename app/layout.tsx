import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Pricty Shopify App | Improve Price Display & Conversions',
  description: 'Pricty helps Shopify stores present prices clearly using smart price blocks. Improve trust, clarity, and conversions without changing product prices.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
